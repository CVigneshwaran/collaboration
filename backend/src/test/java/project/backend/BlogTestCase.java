package project.backend;

import static org.junit.Assert.assertTrue;

import org.junit.BeforeClass;
import org.junit.Test;
import org.springframework.context.ApplicationContext;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

public class BlogTestCase {
/*
	static ApplicationContext context;
	@BeforeClass
	public static void setUpBeforeClass() throws Exception
	{
		context=new AnnotationConfigApplicationContext(DatabaseConfiguration.class);
	}
	
	@Test
	public void testAddBlog()
	{
		Blog bg = new Blog();
		bg.setBlogid(10);
		bg.setBlogname("Shopping Site");
		bg.setBlogcontent("Offers at the rate of 10%");
		bg.setLikes(120);
		bg.setDislike(10);
		bg.setUsername("Vigneshwaran.C");
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
		assertTrue(dao.addBlog(bg));	
	}
	
	@Test
	public void testUpdateBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
		Blog bg = dao.getBlog(10);
		bg.setBlogname("Blog Name");;
		dao.updateBlog(bg);
	}
	
	@Test
	public void testDeleteBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
		Blog bg = dao.getBlog(10);
		bg.setUsername("Vicky");
		dao.deleteblog(bg);
	}
	
	@Test
	public void testGetBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}
	 
	@Test 
	public void testListBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}
	@Test
	public void testListBlogForApproval() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
		
	}
	@Test
	public void testIncrementLikes() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}
	@Test
	public void testIncrementDisLikes() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}
	@Test
	public void testApproveBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}
	@Test
	public void testRejectBlog() {
		BlogDAO dao = (BlogDAO)context.getBean("blogDAO");
	}*/
}
